Singly Linked List Authorization (Lab 2)

This project implements a singly linked list of users with authentication and authorization features.

Features Store users with: Username Password Permissions(using vector) Insert new users into the list Find existing users Authorize users based on their permissions

How to Run:

    Compile the code g++ main.cpp -o lab2
    Run the program ./lab2

